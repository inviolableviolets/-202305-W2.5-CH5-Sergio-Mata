import { playGameOfLife } from "./startGame";

describe("Given a playGameOfLife function", () => {
  describe("When it receives 5 and 5 as parameters", () => {
    test("Then it should start the game", () => {});
  });
});
